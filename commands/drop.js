const { EmbedBuilder, ButtonBuilder, ActionRowBuilder, ButtonStyle } = require('discord.js');
exports.run = async (client, message, args) => { const row = new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('drop_join').setLabel('Participar').setStyle(ButtonStyle.Success)); const sent = await message.channel.send({ embeds: [new EmbedBuilder().setTitle('Drop').setDescription(args.join(' ') || 'Drop aberto!')], components: [row] }); const collector = sent.createMessageComponentCollector({ time: 60000 }); collector.on('collect', interaction => interaction.reply({ content: 'Você entrou no drop!', ephemeral: true })); };
