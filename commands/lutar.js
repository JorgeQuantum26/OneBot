const { ButtonBuilder, ActionRowBuilder, ButtonStyle } = require('discord.js');
exports.run = async (client, message) => { const row = new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('lutar_atacar').setLabel('Atacar').setStyle(ButtonStyle.Primary), new ButtonBuilder().setCustomId('lutar_defender').setLabel('Defender').setStyle(ButtonStyle.Secondary)); const sent = await message.channel.send({ content: '⚔️ Escolha sua ação.', components: [row] }); const collector = sent.createMessageComponentCollector({ time: 30000, filter: interaction => interaction.user.id === message.author.id }); collector.on('collect', interaction => interaction.update({ content: `Ação escolhida: ${interaction.customId}`, components: [] })); };
